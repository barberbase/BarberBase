# V1.5 Law 21 Regression — Push-Absence Correctness Gate

**Unit:** V1.5  
**Gate:** Required before BarberBase ships.  
**Date written:** 2026-06-13  
**Tester:** \***\*\*\*\*\*\*\***\_\_\_\***\*\*\*\*\*\*\***  
**Run start time (IST):** \***\*\*\*\*\*\*\***\_\_\_\***\*\*\*\*\*\*\***

---

## Stated Requirement (verbatim)

> **Law 21:** Push notifications are a convenience layer. Queue correctness is independent
> of push. The BarberBase queue operates correctly with zero push infrastructure.
> If every staff member denies push permission, revokes it, or receives no push
> notifications for an entire shift:
>
> - The queue state machine is unaffected
> - SSE delivers all queue changes to the open dashboard tab
> - All queue mutations succeed via existing REST endpoints
> - No queue entry is stuck, lost, or double-called because push failed
>
> Push delivery, FCM availability, Service Worker execution, and browser notification
> permissions are NEVER in the critical path for queue correctness. They exist only
> for barber convenience when the phone is locked.
>
> Verification test: if push infrastructure is disabled entirely, the product must work
> identically for every core barbershop workflow.

---

## What This Regression Proves

This is a worst-case push-absence test. "Push disabled" means not just denied browser
permissions — it means:

1. **Database:** All `staff_members` rows have `push_enabled=false` and `push_endpoint/
push_p256dh/push_auth=NULL`. The C6.6 EXISTS gate in `CompleteVisitAndCheckout`
   therefore returns `false` and inserts **zero** `web_push.send` outbox_events rows.

2. **Backend env:** `VAPID_PUBLIC_KEY=""`, `VAPID_PRIVATE_KEY=""`, `VAPID_SUBJECT=""`
   (unset or empty). The Go push package cannot sign any VAPID requests.

3. **Frontend env:** `PUBLIC_VAPID_PUBLIC_KEY=""` (unset or empty). The browser
   `pushManager.subscribe()` call receives no valid application server key.

Under these conditions, every core queue workflow must produce **identical** HTTP
response codes, body shapes, PostgreSQL state transitions, and `queue_version`
increments to Run 1 (push enabled). Any divergence is a Law 21 violation.

---

## Prerequisites Checklist

Complete before starting Run 1.

- [ ] `barberbase-core` deployed and reachable. Confirm:
      `bash
curl -s -o /dev/null -w "%{http_code}" https://api.barbers.app/v1/healthz
`
      Expected: `200`
- [ ] `DATABASE_URL` set and reachable from the test machine. Confirm:
      `bash
psql "$DATABASE_URL" -c "SELECT 1"
`
      Expected: 1 row returned.
- [ ] `JWT_SECRET` set in environment for `make test`.
- [ ] Playwright configured to reach the deployed API (`PUBLIC_API_BASE` set in
      `barberbase-frontend/.env`).
- [ ] At least one `staff_members` row with a **seeded test push subscription**:
      `sql
UPDATE staff_members
SET push_enabled   = true,
    push_endpoint  = 'https://fcm.googleapis.com/fcm/send/test-endpoint',
    push_p256dh    = 'test-p256dh-value',
    push_auth      = 'test-auth-value'
WHERE phone_number = '<test_staff_phone>'
  AND is_active    = true;
`
      These do not need to be valid FCM credentials. They only need to be non-NULL
      so the C6.6 EXISTS gate returns `true` in Run 1.
      Record: `test_staff_id = ___________________________`
- [ ] `v1.5-push-state.sh` is executable:
      `bash
chmod +x tests/e2e/v1.5-push-state.sh
bash -n tests/e2e/v1.5-push-state.sh && echo "SYNTAX OK"
`

---

## Push-Disabled Configuration (exact — do not interpret)

Applied by `v1.5-push-state.sh disable`. All of the following must be true
simultaneously for Run 2:

**Database (applied by script):**

```sql
UPDATE staff_members
SET push_enabled  = false,
    push_endpoint = NULL,
    push_p256dh   = NULL,
    push_auth     = NULL
WHERE is_active = true;
```

**Backend environment (set before running Run 2 test suite):**

```
VAPID_PUBLIC_KEY=""
VAPID_PRIVATE_KEY=""
VAPID_SUBJECT=""
```

**Frontend environment:**

```
PUBLIC_VAPID_PUBLIC_KEY=""
```

**Expected runtime behavior in this state:**

- `POST /v1/staff/push/subscribe` (`subscribePush`): returns non-2xx (VAPID unset).
  Must not panic. Must not 500. (This failure is expected and acceptable — see
  Acceptable Differences below.)
- `POST /v1/staff/push/call-next` (`pushCallNext`): not called in Run 2. No push
  notifications are dispatched, so no lock-screen action is ever triggered.
- C6.6 EXISTS gate in `CompleteVisitAndCheckout` step 12.5:
  `EXISTS(SELECT 1 FROM staff_members WHERE location_id=$1 AND push_enabled=true AND is_active=true)`
  evaluates to `false` → zero `web_push.send` outbox_events rows inserted →
  checkout completes normally. This is the **primary Law 21 proof**.

---

## Run 1 — Push Enabled

### Setup

1.1. Confirm VAPID keys are set in the backend environment:
`bash
     echo "VAPID_PUBLIC_KEY: ${VAPID_PUBLIC_KEY:0:20}..."
     echo "VAPID_PRIVATE_KEY: ${VAPID_PRIVATE_KEY:0:20}..."
     echo "VAPID_SUBJECT: $VAPID_SUBJECT"
     `
All three must be non-empty.

1.2. Confirm push-enabled staff exists in DB:
`sql
     SELECT id, push_enabled,
            push_endpoint IS NOT NULL AS endpoint_set,
            push_p256dh   IS NOT NULL AS p256dh_set,
            push_auth     IS NOT NULL AS auth_set
     FROM staff_members
     WHERE is_active = true
       AND push_enabled = true
     LIMIT 3;
     `
Expected: ≥1 row with all flags `true`.

> **Run 1 start time (IST):** \***\*\*\*\*\*\*\***\_\_\_\***\*\*\*\*\*\*\***  
> Record this as `<run_1_start_time>` for the outbox query below.

### Commands

**From `barberbase-core/`:**

```bash
DATABASE_URL=<url> JWT_SECRET=testsecret make test
```

**From `barberbase-frontend/`:**

```bash
npx playwright test
```

### Record — Run 1

| Metric                 | Value  |
| ---------------------- | ------ |
| Core suite total tests | \_\_\_ |
| Core suite passed      | \_\_\_ |
| Core suite failed      | \_\_\_ |
| Playwright total tests | \_\_\_ |
| Playwright passed      | \_\_\_ |
| Playwright failed      | \_\_\_ |

**Run 1 failures (if any):**

| Test name | File | Error |
| --------- | ---- | ----- |
|           |      |       |

**Outbox query — after completeService in Run 1:**

```sql
SELECT COUNT(*)
FROM outbox_events
WHERE type = 'web_push.send'
  AND created_at > '<run_1_start_time>';
```

> **Result (expected ≥ 1 if push-enabled staff present):**
>
> ```
>
> ```

---

## Run 2 — Push Disabled

### Setup

2.1. Apply push-disabled database state:
`bash
     bash tests/e2e/v1.5-push-state.sh disable
     `
Confirm output: `Push disabled: N rows affected.` (N ≥ 1)

2.2. Unset VAPID keys in the backend process environment:
`bash
     export VAPID_PUBLIC_KEY=""
     export VAPID_PRIVATE_KEY=""
     export VAPID_SUBJECT=""
     `
If barberbase-core runs as a systemd service or docker container, update the
env and restart: `systemctl restart barberbase-core` or `docker restart <container>`.
Confirm keys are empty:
`bash
     curl -s https://api.barbers.app/v1/staff/push/subscribe \
       -X POST -H "Content-Type: application/json" \
       -H "Authorization: Bearer <valid_StaffJWT>" \
       -d '{"endpoint":"x","p256dh":"x","auth":"x"}' \
       -o /dev/null -w "%{http_code}"
     `
Expected: non-2xx (4xx or 5xx, but NOT a panic/process crash).

2.3. Unset frontend VAPID key:
`bash
     export PUBLIC_VAPID_PUBLIC_KEY=""
     `
Or set in `barberbase-frontend/.env` before running Playwright.

2.4. Verify push-disabled DB state:
`sql
     SELECT COUNT(*) FROM staff_members
     WHERE push_enabled = true AND is_active = true;
     `
Expected: `0`

> **Run 2 start time (IST):** \***\*\*\*\*\*\*\***\_\_\_\***\*\*\*\*\*\*\***  
> Record this as `<run_2_start_time>` for the critical assertion below.

### Commands (identical to Run 1)

**From `barberbase-core/`:**

```bash
DATABASE_URL=<url> JWT_SECRET=testsecret make test
```

**From `barberbase-frontend/`:**

```bash
npx playwright test
```

### Record — Run 2

| Metric                 | Value  |
| ---------------------- | ------ |
| Core suite total tests | \_\_\_ |
| Core suite passed      | \_\_\_ |
| Core suite failed      | \_\_\_ |
| Playwright total tests | \_\_\_ |
| Playwright passed      | \_\_\_ |
| Playwright failed      | \_\_\_ |

**Run 2 failures — ALL failures must be categorized:**

| Test name | File | Error | Category                       |
| --------- | ---- | ----- | ------------------------------ |
|           |      |       | push-specific / core-violation |

Category definitions:

- **push-specific** = expected failure, listed under Acceptable Differences below.
- **core-violation** = Law 21 violation. Unit FAILS if any row in this category exists.

---

## ★ Critical Assertion — Must Appear in Results

After `completeService` in Run 2, paste this query output verbatim:

```sql
SELECT COUNT(*)
FROM outbox_events
WHERE type = 'web_push.send'
  AND created_at > '<run_2_start_time>';
```

**Expected result: `0`**

> **Paste verbatim output here:**
>
> ```
>
> ```

**Why this is the Law 21 proof:**  
C6.6 gates the `INSERT outbox_events (type='web_push.send')` inside
`CompleteVisitAndCheckout` step 12.5 on:

```sql
EXISTS(
  SELECT 1 FROM staff_members
  WHERE location_id = $1
    AND push_enabled = true
    AND is_active    = true
)
```

With all `staff_members.push_enabled = false`, this EXISTS returns `false`. Zero rows
inserted. The checkout transaction completes normally. `queue_version` increments.
The entry transitions to `completed`. No push path was ever in the critical path.

**PASS / FAIL:** \_\_\_

---

## 10 Core Workflows — Must Pass in BOTH Runs

For each workflow, confirm it passes in Run 1 AND Run 2. If it passes in Run 1 but
fails in Run 2 and is not listed under Acceptable Differences, mark as VIOLATION.

| #   | Workflow          | operationId              | Assertion                                                            | Run 1  | Run 2  | Delta  |
| --- | ----------------- | ------------------------ | -------------------------------------------------------------------- | ------ | ------ | ------ |
| 1   | Join queue        | `joinQueue`              | Entry created; `queue_version` increments                            | \_\_\_ | \_\_\_ | \_\_\_ |
| 2   | Call next         | `callNextCustomer`       | Entry: `waiting→called`; `queue_version` increments                  | \_\_\_ | \_\_\_ | \_\_\_ |
| 3   | Start service     | `startService`           | Entry: `called/waiting→in_progress`; `queue_version` increments      | \_\_\_ | \_\_\_ | \_\_\_ |
| 4   | Complete service  | `completeService`        | Entry: `in_progress→completed`; `queue_version` increments           | \_\_\_ | \_\_\_ | \_\_\_ |
| 5   | Confirm arrival   | `staffConfirmArrival`    | Entry `presence_state→arrived`                                       | \_\_\_ | \_\_\_ | \_\_\_ |
| 6   | Skip              | `skipEntry`              | Entry: `waiting→skipped`; `queue_version` increments                 | \_\_\_ | \_\_\_ | \_\_\_ |
| 7   | No-show           | `markNoShow`             | Entry: `→no_show`; `queue_version` increments                        | \_\_\_ | \_\_\_ | \_\_\_ |
| 8   | Reactivate        | `reactivateEntry`        | Entry: `skipped/no_show→waiting`; `queue_version` increments         | \_\_\_ | \_\_\_ | \_\_\_ |
| 9   | SSE delivery      | `subscribeToQueueStream` | SSE event received for each mutation; event contains `queue_version` | \_\_\_ | \_\_\_ | \_\_\_ |
| 10  | Snapshot recovery | `getQueueSnapshot`       | After disconnect+mutation, snapshot reflects correct queue state     | \_\_\_ | \_\_\_ | \_\_\_ |

**Delta column:** `SAME` (identical result), `EXPECTED-DIFF` (see Acceptable Differences), or `VIOLATION`.

All 10 workflows must show `SAME` or `EXPECTED-DIFF` in the Delta column. Any
`VIOLATION` = unit FAILS.

---

## Acceptable Differences Between Runs (not a Law 21 violation)

The following test failures in Run 2 are **expected and do not constitute a FAIL**:

| Package / Test                                                                                                                                                                                                                                                                                      | Reason acceptable in Run 2                                                                                |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- |
| `internal/push/` — all tests (`TestGenerateAndVerify_RoundTrip`, `TestVerify_ForgedMAC`, `TestVerify_TamperedPayload`, `TestVerify_WrongCommand`, `TestVerify_Expired`, `TestVerify_InvalidFormats`, `TestVerify_ConstantTimePathConfirmed`)                                                        | VAPID/PAT logic tests. Require valid VAPID env. Expected to fail when VAPID_PUBLIC/PRIVATE_KEY are unset. |
| `internal/api/handlers_push_test.go` — all tests (`TestSubscribePush_Success`, `TestSubscribePush_MissingFields`, `TestSubscribePush_DeactivatedStaff`, `TestPushCallNext_ForgedMAC`, `TestPushCallNext_WrongCommand`, `TestPushCallNext_SuccessAndRateLimit`, `TestPushCallNext_DeactivatedStaff`) | Direct calls to `subscribePush` or `pushCallNext`. Expected non-2xx when VAPID unset.                     |
| `internal/outbox/handlers/push_notification_test.go` — all tests                                                                                                                                                                                                                                    | Push outbox dispatch handler. Expected to fail when VAPID signing unavailable.                            |
| Any Playwright test that asserts the push permission prompt's positive (grant) path                                                                                                                                                                                                                 | `PUBLIC_VAPID_PUBLIC_KEY=""` means `pushManager.subscribe()` fails; grant flow tests will fail. Expected. |
| `outbox_events` `web_push.send` row count = 0 in Run 2                                                                                                                                                                                                                                              | This is the **intended** outcome, not a failure. C6.6 EXISTS gate correctly suppresses the insert.        |

**Any other test that passes in Run 1 but fails in Run 2 is a Law 21 violation.**

---

## Results Summary Table

Complete after both runs.

| Acceptance Criterion                                                           | Run 1  | Run 2  | Status |
| ------------------------------------------------------------------------------ | ------ | ------ | ------ |
| Core suite: all non-push tests pass                                            | \_\_\_ | \_\_\_ | \_\_\_ |
| Playwright suite: all non-push tests pass                                      | \_\_\_ | \_\_\_ | \_\_\_ |
| `joinQueue` — entry created, `queue_version` increments                        | \_\_\_ | \_\_\_ | \_\_\_ |
| `callNextCustomer` — `waiting→called`, `queue_version` increments              | \_\_\_ | \_\_\_ | \_\_\_ |
| `startService` — `called→in_progress`, `queue_version` increments              | \_\_\_ | \_\_\_ | \_\_\_ |
| `completeService` — `in_progress→completed`, `queue_version` increments        | \_\_\_ | \_\_\_ | \_\_\_ |
| `staffConfirmArrival` — `presence_state→arrived`                               | \_\_\_ | \_\_\_ | \_\_\_ |
| `skipEntry` — `waiting→skipped`, `queue_version` increments                    | \_\_\_ | \_\_\_ | \_\_\_ |
| `markNoShow` — entry `→no_show`, `queue_version` increments                    | \_\_\_ | \_\_\_ | \_\_\_ |
| `reactivateEntry` — `skipped/no_show→waiting`, `queue_version` increments      | \_\_\_ | \_\_\_ | \_\_\_ |
| `subscribeToQueueStream` — SSE event per mutation with `queue_version`         | \_\_\_ | \_\_\_ | \_\_\_ |
| `getQueueSnapshot` — snapshot correct after disconnect+mutation                | \_\_\_ | \_\_\_ | \_\_\_ |
| **★ Critical:** `web_push.send` outbox COUNT = 0 after completeService (Run 2) | N/A    | \_\_\_ | \_\_\_ |
| No unexpected Run 2 failures outside Acceptable Differences list               | N/A    | \_\_\_ | \_\_\_ |

### Overall Result

> **Run 1:** PASS / FAIL  
> **Run 2:** PASS / FAIL  
> **Law 21 holds:** YES / NO

---

## Restore Push State After Run 2

After Run 2 completes, restore the push flag:

```bash
bash tests/e2e/v1.5-push-state.sh enable
```

Confirm output: `Push re-enabled: N rows affected. Note: subscription data (endpoint/p256dh/auth) was cleared. Re-subscribe via the app.`

Re-seed push subscription data via the app or test fixture — the `enable` command
restores only `push_enabled=true`. The `push_endpoint`, `push_p256dh`, and `push_auth`
columns remain NULL after a `disable/enable` cycle and must be re-populated by a new
browser `POST /v1/staff/push/subscribe` call or by the test seed fixture.

Restore VAPID keys in the backend environment and restart barberbase-core.

---

## Known Limitations and Coverage Gaps

### Test suite coverage of the 10 core workflows

Based on inspection of the existing test files at the time this protocol was written:

| Workflow          | operationId              | Coverage in existing suite                                                                                                                                                                                                      |
| ----------------- | ------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Join queue        | `joinQueue`              | Partial — `TestEnsureAndLockQueueSession_ConcurrentFirstJoiners` covers session locking. Direct HTTP handler test for joinQueue not found in scanned files. **GAP.**                                                            |
| Call next         | `callNextCustomer`       | **Covered** — `TestCallNext_Concurrency`, `TestCallNext_BarberSpecificRoutingExclusion`, `TestCallNext_HybridRouting`, `TestCallNext_WaitingRemoteCount`, `TestCallNext_NoSession`, `TestCallNext_TxRollbackInsertsZeroOutbox`. |
| Start service     | `startService`           | Not found as a dedicated test in scanned files. **GAP.**                                                                                                                                                                        |
| Complete service  | `completeService`        | **Covered** — `TestCompleteVisitAndCheckout_Rollback`, `TestCompleteVisitAndCheckout_NoPushEnabledStaff`, `TestCompleteVisitAndCheckout_WithPushEnabledStaff`. The last two are direct C6.6 gate tests.                         |
| Confirm arrival   | `staffConfirmArrival`    | Not found as a dedicated test in scanned files. **GAP.**                                                                                                                                                                        |
| Skip              | `skipEntry`              | Not found as a dedicated test in scanned files. **GAP.**                                                                                                                                                                        |
| No-show           | `markNoShow`             | Not found as a dedicated test in scanned files. **GAP.**                                                                                                                                                                        |
| Reactivate        | `reactivateEntry`        | Not found as a dedicated test in scanned files. **GAP.**                                                                                                                                                                        |
| SSE delivery      | `subscribeToQueueStream` | **Covered** — `TestSSE_ConcurrentSubscribers`, `TestSSE_DisconnectCleanup`, `TestSSE_RollbackZeroBroadcast`, `TestSSE_MutationAndSnapshotTruth`, `TestSSE_SnapshotNoActiveSession`.                                             |
| Snapshot recovery | `getQueueSnapshot`       | **Covered** — `TestSSE_MutationAndSnapshotTruth`, `TestSSE_SnapshotNoActiveSession`.                                                                                                                                            |

**Known gap:** `joinQueue`, `startService`, `staffConfirmArrival`, `skipEntry`,
`markNoShow`, and `reactivateEntry` do not have dedicated handler-level integration
tests in the scanned files. Per this unit's constraints, new tests are NOT added here —
the gap is recorded for a future coverage unit. During this regression, verify these
workflows manually via API calls and PostgreSQL state inspection if needed.

### Push-specific tests isolatable by package boundary

The following packages contain push-only tests and can be excluded from the Run 2
"unexpected failure" check without manual categorization:

- `internal/push/` — all VAPID/PAT unit tests
- `internal/outbox/handlers/push_notification_test.go` — push dispatch handler
- `internal/api/handlers_push_test.go` — `subscribePush` and `pushCallNext` handlers

Any failure in these packages in Run 2 is pre-classified as acceptable.
Any failure **outside** these packages in Run 2 requires manual categorization.

### Doze-mode and FCM delivery not tested

This regression verifies the server-side Law 21 invariant only (outbox gate and
queue mutation correctness). FCM Doze mode, lock-screen persistence, and
physical device delivery behavior are covered by V1.1, not this unit.
